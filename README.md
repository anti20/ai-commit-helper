# AI Commit Helper

## What it does

Summarizes staged Git changes for commit and PR workflows.

## Install

Install dependencies with `npm install`.

## Usage

Run `ai-commit-helper` after staging your changes with Git.

## Development

Run the CLI locally with `npm run dev`.
